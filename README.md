# Flights Data Pipeline

Simple data pipeline that ingests flight data from API, stores raw JSON, and logs execution.

## Features
- API ingestion (Python + requests)
- Raw data storage (JSON)
- Structured folders (date + time)
- Logging system

## Next Steps
- Upload to Azure
- Data cleaning (Silver layer)
- Data modeling (Gold layer)
